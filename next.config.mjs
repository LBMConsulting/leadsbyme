/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow images from Google (for OAuth profile pics)
  images: {
    domains: ['lh3.googleusercontent.com'],
  },
};

export default nextConfig;
